# Phase 2 Legacy Math Report

## Status

`PARTIALLY_COMPLETE_DART_RUNTIME_NOT_VERIFIED`

## Java client sources audited

- `core/src/model/CRes.java`
- `core/src/model/Fomula.java`
- `core/src/model/Position.java`
- Math call-site context from `CPlayer.java`, `Bullet.java`, `MM.java`, and `CMap.java`

## Server sources compared

- `src/mobiarmy/war/PathSimulator.java`
- `src/mobiarmy/war/Gun.java`
- `src/mobiarmy/war/Boss/bullet/Bullet.java` and bullet subclasses
- `src/mobiarmy/Util.java`

## Implemented

- Exact 91-entry Q10 sine lookup table and derived cosine/tangent tables.
- Legacy `fixangle`, `subangle`, quadrant angle calculation, and `myAngle` behavior.
- Explicit Java signed 32-bit wrap, division, remainder, and shift behavior.
- Integer square root, distance, alpha-based land check, point containment, and inclusive rectangle hit test.
- UI-only interpolation/clamp helpers separated from parity-critical integer math.
- Injectable random wrapper; deterministic gameplay randomness remains server-authoritative.
- Immutable replacement for the overloaded mutable `Position` carrier.
- Golden fixture generated by a compiled Java oracle for all 360 sin/cos inputs, quadrant samples, and distance samples.

## Important compatibility finding

`CRes.fixangle` adjusts by 360 only once. Therefore values outside `[-360, 719]` are not fully normalized. `fixOnce` preserves that behavior for exact parity, while `normalized` is provided separately and must not replace it silently.

## Validation

- Java oracle compiled and executed successfully with `javac/java`.
- Golden fixture contains 360 trigonometry rows, 10 angle rows, and 4 distance rows.
- Dart tests were authored but cannot be run in this environment because Flutter/Dart SDK is unavailable.
- No projectile system was implemented.

## Remaining unresolved

- Exhaustive call-site classification cannot be final where source Markdown has incomplete bodies.
- Random output sequences are intentionally not claimed byte-identical to `java.util.Random`; online gameplay randomness is server-authoritative.
- Projectile convention and server trajectory parity are validated later using captured packets and Phase 9/16 playback.
